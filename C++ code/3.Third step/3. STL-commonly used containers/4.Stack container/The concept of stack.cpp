//stack的基本概念

// 概念：stack是一种 先进后出 (First In Last Out,FILO)的数据结构，它只有一个出口

// 栈中只有 顶端的元素 才可以被外界使用，因此栈不允许有遍历行为

// 栈中进入数据称为  --- 入栈  push

// 栈中弹出数据称为  --- 出栈  pop

//栈中判断容器是否为空 : empty

//栈可以返回元素个数 : size

